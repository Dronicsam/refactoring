import { ThemeComponent } from "shared/config";

export const ActionIconTheme: ThemeComponent = {
    styles: {
        root: {
            '&:hover': {
                background: 'inherit'
            }
        }
    }
}